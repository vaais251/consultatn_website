FROM node:20-alpine AS base

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm ci

# Copy source
COPY . .

# Expose port
EXPOSE 3000

# Development mode
CMD ["npm", "run", "dev"]
